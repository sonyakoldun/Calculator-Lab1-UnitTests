# Лабораторна робота №1: Модульне тестування методу RunEstimate (macOS + SQLite)

## 📋 Зміст
1. [Опис проєкту](#опис-проєкту)
2. [Встановлення та налаштування](#встановлення-та-налаштування)
3. [Створення бази даних SQLite в DBeaver](#створення-бази-даних-sqlite-в-dbeaver)
4. [Запуск тестів](#запуск-тестів)
5. [Структура бази даних](#структура-бази-даних)
6. [Опис тестів](#опис-тестів)

---

## 📌 Опис проєкту

Ця лабораторна робота реалізує **модульне тестування** методу `RunEstimate()` з класу `AnalaizerClass` бібліотеки `AnalizerClassLibrary`.

**Варіант:** №9  
**Метод:** `public static string RunEstimate()`  
**Бібліотека:** `AnalaizerClassLibrary`  
**Платформа:** macOS  
**База даних:** SQLite (через DBeaver)

---

## 🔧 Встановлення та налаштування

### Крок 1: Встановлення необхідних інструментів

#### 1.1 Visual Studio для Mac або Visual Studio Code

**Варіант А: Visual Studio для Mac**
- Завантажте з [visualstudio.com](https://visualstudio.microsoft.com/vs/mac/)
- Встановіть .NET Framework підтримку

**Варіант Б: Visual Studio Code + .NET SDK**
```bash
# Встановіть .NET SDK через Homebrew
brew install dotnet
```

#### 1.2 DBeaver (SQLite клієнт)

**Варіант А: Через Homebrew (рекомендовано)**
```bash
brew install --cask dbeaver-community
```

**Варіант Б: Завантажити з сайту**
- Відкрийте [dbeaver.io/download](https://dbeaver.io/download/)
- Завантажте версію для macOS
- Встановіть DBeaver

#### 1.3 SQLite (зазвичай вже встановлений на macOS)

Перевірте наявність SQLite:
```bash
sqlite3 --version
```

Якщо не встановлений:
```bash
brew install sqlite
```

---

## 🗄️ Створення бази даних SQLite в DBeaver

### Крок 1: Відкриття DBeaver

1. Запустіть **DBeaver**
2. Якщо це перший запуск, створіть нове підключення

### Крок 2: Створення підключення до SQLite

1. Натисніть **"New Database Connection"** (іконка зірочки) або `Cmd+Shift+N`
2. Виберіть **SQLite** зі списку
3. Натисніть **Next**

### Крок 3: Налаштування підключення

1. У полі **"Path"** вкажіть шлях до файлу БД:
   ```
   /Users/ваше_ім'я/Downloads/Calculator/Database/CalculatorTestDB.db
   ```
   
   Або натисніть **"Browse"** та виберіть папку `Database/`, а потім введіть назву файлу: `CalculatorTestDB.db`

2. Натисніть **"Test Connection"** - має з'явитися "Connected"
3. Натисніть **"Finish"**

### Крок 4: Виконання SQL скрипту

1. У DBeaver знайдіть ваше підключення в лівій панелі
2. Клацніть правою кнопкою → **"SQL Editor"** → **"New SQL Script"**
3. Відкрийте файл `Database/CreateTestDatabase.sql`
4. Скопіюйте весь вміст файлу в SQL Editor
5. Натисніть **"Execute SQL Script"** (▶️) або `Cmd+Enter`
6. Переконайтеся, що з'явилося повідомлення про успішне створення

### Крок 5: Перевірка створення

1. Розгорніть ваше підключення в лівій панелі
2. Розгорніть **"Tables"**
3. Має з'явитися таблиця **"TestExpressions"**
4. Клацніть правою кнопкою на таблицю → **"View Data"**
5. Перевірте, що є ~33 записи

---

## ⚙️ Налаштування проєкту

### Крок 1: Встановлення NuGet пакетів

**У Visual Studio для Mac:**
1. Клацніть правою кнопкою на проєкт `AnalizerClassLibrary.Tests`
2. Виберіть **"Manage NuGet Packages"**
3. Перейдіть на вкладку **"Installed"** та перевірте:
   - `MSTest.TestFramework`
   - `System.Data.SQLite.Core`
4. Якщо пакетів немає, перейдіть на вкладку **"Browse"** та встановіть:
   - `System.Data.SQLite.Core` (версія 1.0.118.0 або новіша)

**Через термінал:**
```bash
cd "/Users/ваше_ім'я/Downloads/Calculator/AnalizerClassLibrary.Tests"
dotnet restore
```

### Крок 2: Налаштування App.config

Відкрийте файл `AnalizerClassLibrary.Tests/App.config` та перевірте рядок підключення:

```xml
<connectionStrings>
  <add name="TestDatabase" 
       connectionString="Data Source=CalculatorTestDB.db;Version=3;" 
       providerName="System.Data.SQLite" />
</connectionStrings>
```

**Важливо:** Шлях до файлу БД буде визначатися автоматично. Якщо потрібно вказати абсолютний шлях:

```xml
connectionString="Data Source=/Users/ваше_ім'я/Downloads/Calculator/Database/CalculatorTestDB.db;Version=3;"
```

---

## ▶️ Запуск тестів

### Варіант 1: Visual Studio для Mac

1. Відкрийте `Calculator_Exam_CommandProject.sln`
2. Відкрийте **Test Pad**: `View` → `Pads` → `Unit Tests` (або `Cmd+Shift+T`)
3. Розгорніть `AnalizerClassLibrary.Tests`
4. Клацніть правою кнопкою на проєкт → **"Run All Tests"**
5. Перегляньте результати

### Варіант 2: Через термінал (dotnet test)

```bash
cd "/Users/ваше_ім'я/Downloads/Calculator"
dotnet test AnalizerClassLibrary.Tests/AnalizerClassLibrary.Tests.csproj
```

### Варіант 3: Visual Studio Code

1. Встановіть розширення **".NET Core Test Explorer"**
2. Відкрийте проєкт у VS Code
3. Відкрийте Test Explorer (`Cmd+Shift+P` → "Test: Focus on Test View")
4. Натисніть ▶️ біля тестів

---

## 🗄️ Структура бази даних

### Таблиця: TestExpressions

| Колонка | Тип SQLite | Опис |
|---------|------------|------|
| `Id` | INTEGER (PK, AUTOINCREMENT) | Унікальний ідентифікатор |
| `TestName` | TEXT | Назва тесту |
| `Expression` | TEXT | Вхідний вираз |
| `ExpectedResult` | TEXT | Очікуваний результат |
| `IsError` | INTEGER (0 або 1) | Чи очікується помилка |
| `ErrorCode` | TEXT | Код помилки (якщо IsError = 1) |
| `Description` | TEXT | Опис тесту |
| `TestCategory` | TEXT | Категорія: Valid, DivisionByZero, Overflow, TooManyOperands, EdgeCase |

### Категорії тестів:

1. **Valid** - коректні вирази, які повинні повернути результат
2. **DivisionByZero** - вирази з діленням на 0 (ERROR_09)
3. **Overflow** - вирази, що викликають переповнення int (ERROR_06)
4. **TooManyOperands** - вирази з більш ніж 30 операндами (ERROR_08)
5. **EdgeCase** - граничні випадки (один елемент, нуль, max/min int)

---

## 🧪 Опис тестів

### Тест 1: `RunEstimate_AllTestsFromDatabase`
**Що робить:** Читає всі тестові випадки з SQLite БД і перевіряє їх по черзі.

### Тест 2: `RunEstimate_ValidExpressions`
**Що робить:** Перевіряє тільки коректні вирази (категорія "Valid").

**Приклади:**
- `2+2` → `4`
- `(2+3)*4` → `20`
- `m5` → `-5` (унарний мінус)

### Тест 3: `RunEstimate_DivisionByZero`
**Що робить:** Перевіряє, чи правильно обробляються помилки ділення на 0.

**Очікуваний результат:** Рядок, що починається з `&` і містить `Error 09`

### Тест 4: `RunEstimate_Overflow`
**Що робить:** Перевіряє обробку переповнення int.

**Приклади:**
- `2147483647+1` → `&Error 06...`

### Тест 5: `RunEstimate_TooManyOperands`
**Що робить:** Перевіряє обробку випадку, коли операндів більше 30.

**Очікуваний результат:** `&Error 08`

### Тест 6: `RunEstimate_EdgeCases`
**Що робить:** Перевіряє граничні випадки.

**Приклади:**
- Один елемент: `42` → `42`
- Нуль: `0` → `0`
- Максимальне значення int

---

## ⚠️ Вирішення проблем

### Проблема: "Cannot find SQLite library"

**Рішення:**
```bash
# Встановіть SQLite через Homebrew
brew install sqlite

# Або встановіть NuGet пакет
cd AnalizerClassLibrary.Tests
dotnet add package System.Data.SQLite.Core
```

### Проблема: "Database file not found"

**Рішення:**
1. Перевірте, чи створений файл `CalculatorTestDB.db` у папці `Database/`
2. Перевірте шлях у `App.config`
3. Використайте абсолютний шлях у `connectionString`

### Проблема: "DBeaver не може виконати скрипт"

**Рішення:**
1. Переконайтеся, що вибрано правильне підключення
2. Виконайте команди по одній (виділіть одну команду та натисніть `Cmd+Enter`)
3. Перевірте синтаксис SQL (SQLite має інший синтаксис, ніж MS SQL)

### Проблема: "Tests fail with 'IsError' conversion error"

**Рішення:**
Код вже виправлений - використовується `Convert.ToBoolean()` замість прямого приведення `(bool)`.

---

## 📝 Додавання нових тестів

Щоб додати новий тестовий випадок:

1. Відкрийте DBeaver
2. Підключіться до `CalculatorTestDB.db`
3. Відкрийте SQL Editor
4. Виконайте INSERT:

```sql
INSERT INTO TestExpressions (TestName, Expression, ExpectedResult, IsError, ErrorCode, Description, TestCategory)
VALUES ('MyNewTest', '2+3', '5', 0, NULL, 'Мій новий тест', 'Valid');
```

Тест автоматично з'явиться в `RunEstimate_AllTestsFromDatabase`!

---

## 🔍 Перевірка бази даних в DBeaver

### Перегляд даних:

1. Відкрийте DBeaver
2. Розгорніть підключення → **Tables** → **TestExpressions**
3. Клацніть правою кнопкою → **"View Data"**
4. Перегляньте всі записи

### Запити для перевірки:

```sql
-- Кількість тестів по категоріях
SELECT TestCategory, COUNT(*) as Count
FROM TestExpressions
GROUP BY TestCategory;

-- Всі тести з помилками
SELECT * FROM TestExpressions WHERE IsError = 1;

-- Всі коректні тести
SELECT * FROM TestExpressions WHERE IsError = 0;
```

---

## 📚 Додаткові ресурси

- [SQLite Documentation](https://www.sqlite.org/docs.html)
- [DBeaver Documentation](https://dbeaver.com/docs/)
- [.NET на macOS](https://docs.microsoft.com/en-us/dotnet/core/install/macos)
- [MSTest Documentation](https://docs.microsoft.com/en-us/visualstudio/test/unit-test-basics)

---

**Автор:** Виконано для лабораторної роботи №1  
**Дата:** 2024  
**Варіант:** №9 (метод RunEstimate)  
**Платформа:** macOS + SQLite
