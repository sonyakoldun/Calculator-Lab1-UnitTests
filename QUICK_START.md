# 🚀 Швидкий старт: Як запустити тести

## Крок 1: Створення бази даних (5 хвилин)

1. Відкрийте **SQL Server Management Studio (SSMS)**
2. Підключіться до вашого SQL Server (зазвичай `localhost` або `.\SQLEXPRESS`)
3. Відкрийте файл: `Database/CreateTestDatabase.sql`
4. Натисніть **F5** (або кнопку "Execute")
5. Переконайтеся, що з'явилось повідомлення: "База даних CalculatorTestDB створена успішно!"

## Крок 2: Налаштування підключення (2 хвилини)

1. Відкрийте файл: `AnalizerClassLibrary.Tests/App.config`
2. Знайдіть рядок з `connectionString`
3. Якщо потрібно, змініть `Data Source` на назву вашого SQL Server:
   - `localhost` - стандартний SQL Server
   - `.\SQLEXPRESS` - SQL Server Express
   - `(local)` - альтернативний варіант

**Приклад:**
```xml
connectionString="Data Source=localhost;Initial Catalog=CalculatorTestDB;..."
```

## Крок 3: Відкриття проєкту (1 хвилина)

1. Відкрийте файл: `Calculator_Exam_CommandProject.sln` у Visual Studio
2. Дочекайтеся, поки Visual Studio завантажить всі проєкти

## Крок 4: Запуск тестів (1 хвилина)

### Варіант А: Через Test Explorer (рекомендовано)

1. Натисніть: `Test` → `Test Explorer` (або `Ctrl+E, T`)
2. Натисніть кнопку **"Run All"** (▶️)
3. Дочекайтеся завершення тестів
4. Перегляньте результати: ✅ = пройшов, ❌ = не пройшов

### Варіант Б: Через меню

1. Натисніть: `Test` → `Run` → `All Tests` (або `Ctrl+R, A`)

## Крок 5: Перевірка результатів

Після запуску тестів ви побачите:

```
✅ RunEstimate_AllTestsFromDatabase
✅ RunEstimate_ValidExpressions
✅ RunEstimate_DivisionByZero
✅ RunEstimate_Overflow
✅ RunEstimate_TooManyOperands
✅ RunEstimate_EdgeCases
✅ RunEstimate_SimpleAddition_ReturnsCorrectResult
... (і так далі)
```

**Якщо всі тести пройшли:** 🎉 Вітаю! Все працює!

**Якщо є помилки:** Дивіться розділ "Вирішення проблем" нижче.

---

## ❓ Вирішення проблем

### Проблема: "Cannot connect to database"

**Рішення:**
1. Перевірте, чи запущений SQL Server
2. Перевірте рядок підключення в `App.config`
3. Переконайтеся, що база даних `CalculatorTestDB` існує

### Проблема: "Test fails with format error"

**Рішення:**
- Це нормально для тестів з некоректними виразами
- Перевірте, чи тест очікує помилку (`IsError = 1`)

### Проблема: "MSTest references not found"

**Рішення:**
1. `Tools` → `NuGet Package Manager` → `Restore NuGet Packages`
2. Або встановіть "Visual Studio Test Tools" через Visual Studio Installer

---

## 📊 Що тестується?

Тести перевіряють метод `RunEstimate()`, який обчислює математичні вирази:

✅ **Коректні вирази:**
- `2+2` → `4`
- `(2+3)*4` → `20`
- `m5` → `-5` (унарний мінус)

❌ **Помилки:**
- `10/0` → `&Error 09` (ділення на 0)
- `2147483647+1` → `&Error 06` (переповнення)

---

## 📚 Детальні інструкції

Для більш детальної інформації дивіться:
- `README_TESTS.md` - повна документація
- `REPORT_TEMPLATE.md` - шаблон звіту
- `GITHUB_INSTRUCTIONS.md` - як завантажити на GitHub

---

**Готово!** Тепер ви можете запускати тести та писати звіт! 🎓
