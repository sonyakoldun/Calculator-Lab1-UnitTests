# 🚀 Швидкий старт: macOS + SQLite + DBeaver

## Крок 1: Встановлення DBeaver (5 хвилин)

```bash
# Через Homebrew (рекомендовано)
brew install --cask dbeaver-community
```

Або завантажте з [dbeaver.io/download](https://dbeaver.io/download/)

## Крок 2: Створення бази даних в DBeaver (10 хвилин)

### 2.1 Створення підключення

1. Запустіть **DBeaver**
2. Натисніть **"New Database Connection"** (⭐) або `Cmd+Shift+N`
3. Виберіть **SQLite** → **Next**
4. У полі **"Path"** вкажіть:
   ```
   /Users/ваше_ім'я/Downloads/Calculator/Database/CalculatorTestDB.db
   ```
   (або натисніть **Browse** та виберіть папку `Database/`)
5. Натисніть **"Test Connection"** → має бути "Connected"
6. Натисніть **"Finish"**

### 2.2 Виконання SQL скрипту

1. У DBeaver клацніть правою кнопкою на ваше підключення
2. **SQL Editor** → **New SQL Script**
3. Відкрийте файл `Database/CreateTestDatabase.sql`
4. Скопіюйте весь вміст в SQL Editor
5. Натисніть **"Execute SQL Script"** (▶️) або `Cmd+Enter`
6. Перевірте, що з'явилося повідомлення про успіх

### 2.3 Перевірка

1. Розгорніть підключення → **Tables** → **TestExpressions**
2. Клацніть правою кнопкою → **"View Data"**
3. Має бути ~33 записи ✅

## Крок 3: Налаштування проєкту (2 хвилини)

### 3.1 Встановлення NuGet пакетів

**У Visual Studio для Mac:**
1. Клацніть правою кнопкою на `AnalizerClassLibrary.Tests`
2. **Manage NuGet Packages**
3. Перевірте наявність:
   - `MSTest.TestFramework`
   - `System.Data.SQLite.Core`
4. Якщо немає - встановіть `System.Data.SQLite.Core`

**Через термінал:**
```bash
cd "/Users/ваше_ім'я/Downloads/Calculator"
dotnet restore
```

### 3.2 Перевірка App.config

Відкрийте `AnalizerClassLibrary.Tests/App.config` - має бути:

```xml
<connectionStrings>
  <add name="TestDatabase" 
       connectionString="Data Source=CalculatorTestDB.db;Version=3;" 
       providerName="System.Data.SQLite" />
</connectionStrings>
```

## Крок 4: Запуск тестів (1 хвилина)

### Варіант А: Visual Studio для Mac

1. Відкрийте `Calculator_Exam_CommandProject.sln`
2. `View` → `Pads` → `Unit Tests` (або `Cmd+Shift+T`)
3. Клацніть правою кнопкою на `AnalizerClassLibrary.Tests`
4. **"Run All Tests"**

### Варіант Б: Термінал

```bash
cd "/Users/ваше_ім'я/Downloads/Calculator"
dotnet test AnalizerClassLibrary.Tests/AnalizerClassLibrary.Tests.csproj
```

## Крок 5: Перевірка результатів

Після запуску тестів ви побачите:

```
✅ RunEstimate_AllTestsFromDatabase
✅ RunEstimate_ValidExpressions
✅ RunEstimate_DivisionByZero
✅ RunEstimate_Overflow
✅ RunEstimate_TooManyOperands
✅ RunEstimate_EdgeCases
... (і так далі)
```

**Якщо всі тести пройшли:** 🎉 Вітаю! Все працює!

**Якщо є помилки:** Дивіться розділ "Вирішення проблем" нижче.

---

## ❓ Вирішення проблем

### Проблема: "Cannot find SQLite library"

```bash
# Встановіть SQLite
brew install sqlite

# Або встановіть NuGet пакет
cd AnalizerClassLibrary.Tests
dotnet add package System.Data.SQLite.Core
```

### Проблема: "Database file not found"

1. Перевірте, чи створений файл `CalculatorTestDB.db` у папці `Database/`
2. Перевірте шлях у `App.config`
3. Використайте абсолютний шлях:
   ```xml
   connectionString="Data Source=/Users/ваше_ім'я/Downloads/Calculator/Database/CalculatorTestDB.db;Version=3;"
   ```

### Проблема: "DBeaver не може виконати скрипт"

1. Переконайтеся, що вибрано правильне підключення
2. Виконайте команди по одній (виділіть одну команду → `Cmd+Enter`)
3. Перевірте синтаксис SQL

---

## 📊 Що тестується?

Тести перевіряють метод `RunEstimate()`, який обчислює математичні вирази:

✅ **Коректні вирази:**
- `2+2` → `4`
- `(2+3)*4` → `20`
- `m5` → `-5` (унарний мінус)

❌ **Помилки:**
- `10/0` → `&Error 09` (ділення на 0)
- `2147483647+1` → `&Error 06` (переповнення)

---

## 📚 Детальні інструкції

Для більш детальної інформації дивіться:
- `README_TESTS_MACOS.md` - повна документація для macOS
- `REPORT_TEMPLATE.md` - шаблон звіту
- `GITHUB_INSTRUCTIONS.md` - як завантажити на GitHub

---

**Готово!** Тепер ви можете запускати тести та писати звіт! 🎓
